{
  "name": "lagom-game-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "deploy": "tsc && vite build && butler push dist quackqack/ld54:html5",
    "preview": "vite preview"
  },
  "dependencies": {
    "lagom-engine": "1.0.0",
    "pixi.js": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
